# Everything in bin/ except qs is a multicall symlink onto it, one per tool in
# Quickshell.app/Contents/Resources/tools, generated by qs-bundle from
# src/tools/. Only the command itself is committed.
*
!qs
!.gitignore
