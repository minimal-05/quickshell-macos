// Quickshell.Wayland shim for macOS — ShortcutsInhibitor
//
// Alias of ShortcutInhibitor (the real upstream element name is singular).
// Provided only so an import of the plural spelling resolves. Fully inert —
// see ShortcutInhibitor.qml for why.

import QtQuick

ShortcutInhibitor {}
